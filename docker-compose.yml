services:
  junction-box:
    build: .
    ports:
      - "3001:3001"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - junction-box-data:/app/data
    restart: unless-stopped

volumes:
  junction-box-data:
