services:
  gonude:
    build: .
    ports:
      - "3001:3001"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - gonude-data:/app/data
    restart: unless-stopped

volumes:
  gonude-data:
